# HuggingFace ecosystem for training
diffusers==0.36.0
accelerate>=1.2.0
transformers>=4.47.0
peft>=0.14.0
safetensors>=0.4.0
datasets>=3.0.0

# Image processing
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# WD14 tagger (CPU-only onnxruntime — GPU version lacks ARM64 wheels)
onnxruntime>=1.19.0
huggingface_hub>=0.27.0
numpy>=1.26.0
pandas>=2.1.0

# Tokenizer backends (required by some SDXL text encoders)
sentencepiece>=0.2.0

# Training utilities
prodigyopt>=1.0
bitsandbytes>=0.45.0
wandb>=0.18.0

# Config
toml>=0.10.2

# HTTP client for ComfyUI API
requests>=2.31.0
websocket-client>=1.7.0
